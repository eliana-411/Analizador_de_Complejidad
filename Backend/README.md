# Para instalar dependencias para el backend, seguir el paso a paso:

## Instalar uv, en powershell ejecutar:
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

## Reiniciar visual studio code

## Ejecutar dentro de Backend\:
uv sync

## Se creará un .venv. De esta forma se instalaran los paquetes utilizados para correr el backend.