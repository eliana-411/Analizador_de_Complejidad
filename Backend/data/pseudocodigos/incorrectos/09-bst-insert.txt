Nodo {valor izquierdo derecho}

insertar(Nodo raiz, valor)
begin
    Nodo nuevo

    if raiz = NULL then
    begin
        nuevo.valor 🡨 valor
        nuevo.izquierdo 🡨 NULL
        nuevo.derecho 🡨 NULL
        return nuevo
    end

    if (valor < raiz.valor) then
    begin
        raiz.izquierdo 🡨 insertar(raiz.izquierdo, valor)
    else
    begin
        raiz.derecho 🡨 insertar(raiz.derecho, valor)
    end

    return raiz
end
