potencia(base, exp)
begin
    if (exp == 0) then
    begin
        return 1
    end
    else
    begin
        if (exp MOD 2 == 0) then
        begin
            mitad ← CALL potencia(base, exp / 2)
            return mitad * mitad
        end
        else
        begin
            temp ← CALL potencia(base, exp - 1)
            return base * temp
        end
    end
end